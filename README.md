What I've installed:

# As the required packages (For what?)
apt-transport-https
ca-certificates
curl
gnupg
lsb-release

# For the Docker
docker-ce
docker-ce-cli
containerd.io
docker-compose-plugin


permission denied while trying to connect to the docker API at unix:///var/run/docker.sock 
